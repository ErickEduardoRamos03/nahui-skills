import { ref, watch } from 'vue'
export function useLocal(key, fallback){let initial=fallback;try{const raw=localStorage.getItem(key);if(raw!==null)initial=JSON.parse(raw)}catch{}const value=ref(initial);watch(value,v=>localStorage.setItem(key,JSON.stringify(v)),{deep:true});return value}
export function exportData(){const payload={schema:1,exportedAt:new Date().toISOString(),settings:JSON.parse(localStorage.getItem('nahui-settings')||'{}'),progress:JSON.parse(localStorage.getItem('nahui-progress')||'{}'),writing:JSON.parse(localStorage.getItem('nahui-writing')||'{}')};const a=document.createElement('a');a.href=URL.createObjectURL(new Blob([JSON.stringify(payload,null,2)],{type:'application/json'}));a.download='nahui-skills-backup.json';a.click();setTimeout(()=>URL.revokeObjectURL(a.href),1000)}
export async function importData(file){const obj=JSON.parse(await file.text());if(obj.schema!==1)throw new Error('Respaldo incompatible');localStorage.setItem('nahui-settings',JSON.stringify(obj.settings||{}));localStorage.setItem('nahui-progress',JSON.stringify(obj.progress||{}));localStorage.setItem('nahui-writing',JSON.stringify(obj.writing||{}));location.reload()}
